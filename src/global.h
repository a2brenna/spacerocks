#ifndef __GLOBAL_H__
#define __GLOBAL_H__

#include <cstddef>

extern size_t CONFIG_SCREEN_WIDTH;
extern size_t CONFIG_SCREEN_HEIGHT;
extern size_t UNITS_PER_PIXEL;

extern bool CONFIG_VSYNC;
extern size_t THRUST;
extern size_t MUZZLE_VELOCITY;
extern const double PI;

#endif
